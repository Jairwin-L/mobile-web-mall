export enum MAIN {
  HOME = '/goods',
}

export enum ADDRESS {
  LIST = '/address',
  SHOW = '/address/detail',
  CREATE = '/address',
  EDIT = '/address',
  DEL = '/address',
}

export enum COLLECTION {
  LIST = '/collection',
  CREATE = '/collection',
  DEL = '/collection',
}

export enum SHOP {
  LIST = '/shop',
  DEL = '/shop',
  SHOW = '/shop',
}

export enum CATEGORY {
  LIST = '/category',
}

export enum ORDER {
  LIST = '/order',
}

export enum AUTH {
  CHANGE_PASSWORD = '/change-password',
  LOGIN = '/login',
  REGISTER = '/register',
}
