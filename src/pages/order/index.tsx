import { PageLayout } from '@/components';

export default function Order() {
  return (
    <PageLayout
      initData={{}}
      extraInfo={{
        navbarTitle: '订单',
      }}
    >
      订单
    </PageLayout>
  );
}
