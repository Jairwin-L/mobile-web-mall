import { PageLayout } from '@/components';

const Collection = () => {
  return (
    <PageLayout
      extraInfo={{
        navbarTitle: '登录',
      }}
    >
      收藏
    </PageLayout>
  );
};

export default Collection;
